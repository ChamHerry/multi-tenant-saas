-- Multi-tenant SaaS template baseline requires only built-in PostgreSQL 16 features.
-- gen_random_uuid() is available from pg_catalog in PostgreSQL 16, so no project
-- extension is installed here.
SELECT 1;
