-- Removed from the reusable multi-tenant SaaS template.
-- This version previously introduced shared repository, graph, symbol, and vector
-- search tables for the RepoMind product domain. Those tables are intentionally
-- not part of the template baseline.
SELECT 1;
