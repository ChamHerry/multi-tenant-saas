-- Tenant and organization are the same public entity in RepoMind.
-- This version intentionally keeps public.tenants as the single metadata table;
-- tenant schema/graph runtime is provisioned lazily by application code.
SELECT 1;
